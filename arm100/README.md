# ARM100

`robot/step/so100/` and `robot/step/so101/` contain the editable arm models.
Common parts use descriptive filenames. Leader-only and follower-only parts are
kept in the same folder with `leader_` and `follower_` prefixes.
